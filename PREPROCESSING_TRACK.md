# Preprocessing Track

## Current Goal

Build a clean, reproducible project for a CNN + Transformer remote sensing model using true Sentinel-2 patch time series.

Main training tensor:

```text
patches: [N, T, 12, 15, 15]
```

No hand-engineered tabular collapse is used as the main dataset.

## What Was Done From The Beginning

1. Read the raw competition files and identified the main label file: `downloadedRawData/points_train_label.csv`.
2. Identified raw image folders: `downloadedRawData/region_train_1`, `downloadedRawData/region_train_2`, `downloadedRawData/region_train_3`, `downloadedRawData/region_train_4`.
3. Parsed training labels into unique point locations plus phenophase/crop labels.
4. Audited TIFF filenames across all region folders.
5. Normalized known filename formatting problems in parsing logic so malformed region separators do not break indexing.
6. Detected unsupported image products/bands and excluded them from the configured 12-band tensor.
7. Detected duplicate canonical files and kept one deterministic version per duplicate group.
8. Built a TIFF inventory by `region_id`, acquisition date, band, processing level, and path.
9. Selected one file per `region/date/band`, preferring `L2A` over `L1C`, then stable path ordering.
10. Read GeoTIFF metadata with `rasterio` because PIL reads these float TIFF values incorrectly in this environment.
11. Built region bounding boxes from raster metadata.
12. Checked each point against each region bounding box by script, not manually one by one.
13. Resolved points that matched multiple regions by choosing the best deterministic region using time steps, band count, border margin, then region id.
14. Dropped points outside all training regions.
15. Converted longitude/latitude to pixel coordinates.
16. Extracted true `15x15` patches centered on each point for each valid timestep and configured band.
17. Used edge replication when a patch crossed an image border.
18. Detected invalid pixels using the configured rule: finite and `0.0 < value <= 2.0`.
19. Preserved valid raw values and filled invalid tensor positions with `0.0` only while recording `valid_pixel_mask`.
20. Saved the clean patch dataset to `artifacts/patches_clean/train_cnn_transformer_15x15.npz`.
21. Saved train-only normalization statistics to `artifacts/normalization/train_patch_band_stats.json`.
22. Added deterministic train/validation split generation to `artifacts/splits/train_val_split.csv`.
23. Replaced weak SVG charts with PNG report charts generated by matplotlib.
24. Replaced biased sample patch selection with deterministic reservoir sampling across `random`, `invalid`, and `edge` groups.
25. Added a notebook runner: `notebooks/project_pipeline.ipynb`.
26. Added a modular CNN + Transformer baseline model scaffold.
27. Added test-time patch extraction using the same preprocessing core as training.
28. Added a generic `result.json` writer, with a clear warning that the exact submission schema still needs the official guide restored/confirmed.

## Dataset Schema

The NPZ dataset contains:

```text
patches
valid_pixel_mask
band_mask
time_mask
time_doy
time_dates
border_margin_pixels
center_clamped
source_file_index
band_valid_ratio
point_id
longitude
latitude
resolved_region_id
bands
schema_version
invalid_fill_value
valid_min_exclusive
valid_max_inclusive
patch_size
```

Training-only label arrays:

```text
crop_type_id
crop_type_names
phenophase_names
phenophase_doy
```

## Mask Meaning

`valid_pixel_mask`:

```text
True where raw pixel value is finite and inside the configured valid range.
False where raw value was nodata/corrupt/extreme/outside valid range.
```

`band_mask`:

```text
True where that sample/timestep/band was actually read and extracted.
False where the region/date/band was missing or unreadable.
```

`time_mask`:

```text
True where the sample has a real acquisition timestep.
False where the tensor is padded to the max T length.
```

## Normalization Policy

Raw clean patch tensors are not normalized in-place.

The pipeline saves:

```text
artifacts/patches_clean/train_cnn_transformer_15x15.npz
artifacts/normalization/train_patch_band_stats.json
```

The model applies normalization at training/inference time using training-only statistics and masks invalid pixels back to zero after normalization.

## Reporting Policy

The report folder is regenerated by:

```bash
python3 scripts/preprocess_data.py --config configs/preprocessing.json
```

Important files:

```text
artifacts/preprocessing_report/preprocessing_log.md
artifacts/preprocessing_report/steps_one_by_one.md
artifacts/preprocessing_report/summary.json
artifacts/preprocessing_report/chart_manifest.json
artifacts/preprocessing_report/charts/
artifacts/preprocessing_report/samples/
```

Patch samples are deterministic with seed `42` and grouped as:

```text
random
invalid
edge
```

This avoids the old problem where the examples looked random but were actually biased toward early or invalid cells.

## Training-Time And Test-Time Alignment

Training dataset creation uses:

```text
preprocessing.dataset.build_patch_dataset(..., mode="train")
```

Final test extraction uses the same core function:

```text
preprocessing.dataset.build_patch_dataset(..., mode="test")
```

The only intended difference is that test mode can use nearest-region fallback if needed for inference robustness. The patch extraction, masks, band order, valid value thresholds, and border handling are shared.

## Known Risks

1. The exact final `result.json` schema is not confirmed because the guide folder was removed from the workspace.
2. PyTorch is installed in the local `.venv` and the baseline smoke test runs on CPU; use Colab/GPU for serious training.
3. The valid range `0.0 < value <= 2.0` is conservative for the current float reflectance-like TIFF reads, but if the platform provides scaled integer reflectance, the config must be changed.
4. Some samples contain many invalid pixels or missing date-band cells; the model must use masks and avoid treating filled zeros as normal data.
5. Center clamping can happen when a point is very near or slightly outside a date-specific raster footprint; this is logged in metadata.

## Current Best Next Direction

1. Run `notebooks/project_pipeline.ipynb` in Colab.
2. Confirm the new report charts and sample panels.
3. Install requirements in Colab and run a 1-epoch baseline smoke test.
4. Restore/confirm the official submission guide and update only `inference/write_submission.py` if the JSON schema differs.
5. Then improve the model experimentally: stronger CNN encoder, temporal augmentations, loss weighting, ensembling, and crop-specific phenophase heads.

## Latest Verification Run

Ran after the project cleanup/refactor:

```bash
PYTHONPYCACHEPREFIX=/tmp/pycache MPLCONFIGDIR=/tmp/matplotlib python3 scripts/preprocess_data.py --config configs/preprocessing.json
PYTHONPYCACHEPREFIX=/tmp/pycache MPLCONFIGDIR=/tmp/matplotlib .venv/bin/python tests/test_preprocessing_light.py
PYTHONPYCACHEPREFIX=/tmp/pycache MPLCONFIGDIR=/tmp/matplotlib .venv/bin/python scripts/train_baseline.py --config configs/train_baseline.json --epochs 1
```

Outputs from the preprocessing run:

```text
patch_shape: [778, 29, 12, 15, 15]
samples_kept: 778
missing_band_cells: 367
global_valid_pixel_ratio: 0.9231752262566787
edge_replication_samples: 136
sample_groups_kept: random=4, invalid=4, edge=4
train_split: 609
val_split: 169
split_mode: grouped by resolved_region_id
train_regions: 41
val_regions: 8
```

Baseline smoke run output:

```text
device: cpu
epochs: 1
model: artifacts/models/cnn_transformer_baseline/model.pt
history: artifacts/models/cnn_transformer_baseline/history.json
```

The 1-epoch metric is only a smoke test, not a final scientific result. The split is now grouped by resolved region, but high validation crop accuracy can still be optimistic because crop type may be strongly region-correlated in this small dataset. For serious experiments, compare several region-held-out seeds and inspect class balance.


## Colab GPU Config Added

Added:

```text
configs/model_cnn_transformer_large.json
configs/train_colab_gpu.json
```

The GPU config enables:

```text
amp: true
scheduler: cosine
warmup_epochs: 5
early_stopping_patience: 15
save_best_only: true
batch_size: 32
epochs: 80
```

If Colab GPU memory is not enough, reduce `batch_size` first. If training is unstable, reduce `learning_rate` from `0.0002` to `0.0001`.


## Raw Data Move

Created:

```text
downloadedRawData/
```

Moved into that folder:

```text
downloadedRawData/points_train_label.csv
downloadedRawData/region_train_1/
downloadedRawData/region_train_2/
downloadedRawData/region_train_3/
downloadedRawData/region_train_4/
```

No raw TIFF contents or label CSV contents were edited in this move. Only file locations changed. `configs/preprocessing.json` was updated to use the new paths.

Raw data size is about `70GB`, so `downloadedRawData/` is ignored by Git. The compact final NPZ is about `83MB` and is configured as trackable if you want Colab to train from the GitHub repo.


## Training Metric Fix

The training log now reports `phenophase_mae_days`. This is more meaningful than only looking at normalized phenophase loss. Crop accuracy can still look very high, so compare `val_phenophase_mae_days` against the simple constant train-mean baseline from `scripts/audit_training_data.py`.


## PDF-Aligned Query Classifier

After checking the final-round PDF, the correct prediction unit is a point-date query:

```text
"Longitude_Latitude_Date": ["CropType", "PhenophaseStage"]
```

So the old date-regression baseline is not the final task model. Added a corrected query classifier:

```text
data/query_dataset_npz.py
models/query_cnn_transformer.py
training/query_engine.py
scripts/train_query_classifier.py
configs/model_query_cnn_transformer.json
configs/train_query_colab_gpu.json
```

Use this for real training:

```bash
python3 scripts/train_query_classifier.py --config configs/train_query_colab_gpu.json
```

Local CPU smoke test completed for 1 epoch, but local PyTorch does not see Apple MPS in this environment, so full training should be run on Colab GPU.

## 2026-04-13 PDF Recheck And Correction

1. Rechecked `Round2 Project Submission Manual_en_0407.pdf`.
2. Confirmed the final platform is inference-only, not a training environment.
3. Confirmed the runtime input is expected under `/input`, mainly `test_point.csv` or `points_test.csv` plus `/input/region_test/*.tiff`.
4. Confirmed the final output must be `/output/result.json`.
5. Confirmed our old `scripts/run_inference.py` was not enough for the final task because it wrote a generic point-level JSON instead of the required point-date key format.
6. Added `scripts/run_query_inference.py` as the final-round inference entry point. It extracts test patches with the same preprocessing core, runs the query-date CNN+Transformer model, and writes point-date JSON.
7. Added `inference/query_predict.py` for query-date inference using `query_doy` plus the extracted patch time series.
8. Added `write_point_date_result_json` in `inference/write_submission.py`. It writes keys like `Longitude_Latitude_Date` and values like `[CropType, PhenophaseStage]`.
9. Added `run.sh` and `Dockerfile` following the manual's `/workspace`, `/input`, and `/output` structure.
10. Added support for minor test CSV column variants such as `Longtitude`, `lon`, `lat`, `date`, and missing `point_id` fallback.
11. Added a date-ablation model config with `use_query_doy=false`: `configs/model_query_cnn_transformer_no_date.json` and `configs/train_query_colab_gpu_no_date.json`.
12. Reason for the ablation: a date-only baseline already predicts rice phenophase stage perfectly on the current validation split, so the high rice-stage validation score is mostly a calendar effect and should not be treated as proof that the image model learned phenology from pixels.
13. Practical interpretation: crop prediction still needs imagery. Phenophase stage may be strongly determined by query date in this dataset, but we should report that clearly and compare against the no-date ablation before claiming image-based stage learning.
14. Remaining requirement before official submission: a trained query-model checkpoint must be included at `artifacts/models/query_cnn_transformer_colab/model.pt` or supplied with `CHECKPOINT_PATH`; the competition platform will not train it for us.
