{
  "dataset_npz": "artifacts/patches_clean/train_cnn_transformer_15x15.npz",
  "split_csv": "artifacts/splits/train_val_split.csv",
  "output_dir": "artifacts/models/xgboost_ultimate_a100",
  "seed": 42,
  "rice_stage_loss_only": true,
  "disabled_bands": [],
  "aux_feature_set": "phenology",
  "max_timesteps": 29,
  "n_jobs": 16,
  "early_stopping_rounds": 150,
  "verbose": true,
  "xgboost": {
    "tree_method": "hist",
    "device": "cuda",
    "n_estimators": 3000,
    "learning_rate": 0.025,
    "max_depth": 6,
    "min_child_weight": 2.0,
    "subsample": 0.9,
    "colsample_bytree": 0.85,
    "reg_lambda": 4.0,
    "reg_alpha": 0.05,
    "max_bin": 512,
    "eval_metric": "mlogloss"
  },
  "note": "Deterministic heavy XGBoost baseline: flattened temporal band medians, valid ratios, time masks, query DOY, and phenology aux features. Trains crop on all rows and stage on rice-supervised rows."
}
